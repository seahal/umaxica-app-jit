pre-commit:
  commands:
    audit:
      run: bundle exec bundle audit
    rubocop:
      glob: "*.{rb,rake,ru,gemspec}"
      run: bundle exec rubocop -A
pre-push:
  commands:
    tests:
      run: bin/rails test all
    bun:
      run: bun test
