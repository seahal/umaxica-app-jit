# == Schema Information
#
# Table name: customer_email_statuses
# Database name: guest
#
#  id :bigint           not null, primary key
#
unverified:
  id: <%= CustomerEmailStatus::UNVERIFIED %>

verified:
  id: <%= CustomerEmailStatus::VERIFIED %>

suspended:
  id: <%= CustomerEmailStatus::SUSPENDED %>

deleted:
  id: <%= CustomerEmailStatus::DELETED %>

nothing:
  id: <%= CustomerEmailStatus::NOTHING %>

unverified_with_sign_up:
  id: <%= CustomerEmailStatus::UNVERIFIED_WITH_SIGN_UP %>

verified_with_sign_up:
  id: <%= CustomerEmailStatus::VERIFIED_WITH_SIGN_UP %>

oauth_linked:
  id: <%= CustomerEmailStatus::OAUTH_LINKED %>
