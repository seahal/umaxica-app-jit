# == Schema Information
#
# Table name: org_document_audit_events
#
#  id :string(255)      default("NONE"), not null, primary key
#

CREATED:
  id: CREATED

UPDATED:
  id: UPDATED

DELETED:
  id: DELETED
