# == Schema Information
#
# Table name: user_identity_social_google_statuses
#
#  id :string(255)      not null, primary key
#
active:
  id: "ACTIVE"
revoked:
  id: "REVOKED"
deleted:
  id: "DELETED"
