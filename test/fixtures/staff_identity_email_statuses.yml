# == Schema Information
#
# Table name: staff_identity_email_statuses
#
#  id :string(255)      default("UNVERIFIED"), not null, primary key
#

none:
  id: "NONE"
unverified:
  id: "UNVERIFIED"
verified:
  id: "VERIFIED"
suspended:
  id: "SUSPENDED"
deleted:
  id: "DELETED"
