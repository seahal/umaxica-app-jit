# == Schema Information
#
# Table name: user_identity_one_time_password_statuses
#
#  id :string           not null, primary key
#

active:
  id: "ACTIVE"
inactive:
  id: "INACTIVE"
revoked:
  id: "REVOKED"
deleted:
  id: "DELETED"
