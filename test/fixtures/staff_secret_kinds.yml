# == Schema Information
#
# Table name: staff_secret_kinds
# Database name: operator
#
#  id :string(255)      not null, primary key
#

unlimited:
  id: UNLIMITED

one_time:
  id: ONE_TIME

time_bound:
  id: TIME_BOUND
