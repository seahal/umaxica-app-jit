# == Schema Information
#
# Table name: staff_identity_secret_statuses
#
#  id :string(255)      not null, primary key
#

active:
  id: "ACTIVE"

used:
  id: "USED"

expired:
  id: "EXPIRED"

revoked:
  id: "REVOKED"

deleted:
  id: "DELETED"
