# == Schema Information
#
# Table name: staff_audit_events
# Database name: audit
#
#  id   :bigint           not null, primary key
#  code :citext           not null
#
# Indexes
#
#  index_staff_audit_events_on_code  (code) UNIQUE
#

login_success:
  code: "11 # was 11 # was LOGIN_SUCCESS"

neyo:
  code: "NEYO"

authorization_failed:
  code: "12 # was 12 # was AUTHORIZATION_FAILED"

logged_in:
  code: "13 # was 13 # was LOGGED_IN"

logged_out:
  code: "14 # was 14 # was LOGGED_OUT"

login_failed:
  code: "15 # was 15 # was LOGIN_FAILED"

token_refreshed:
  code: "16 # was 16 # was TOKEN_REFRESHED"
