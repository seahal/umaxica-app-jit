<div>
  <h1 class="font-bold text-4xl"><%= t('www.app.setting.totp.index.title') %></h1>
  <p>Find me in app/views/net/session/apples/new.html.erb</p>
  <ul>
    <% @utbotp.each do |t| %>
      <li> <%= t.id %></li>
    <% end %>
  </ul>
  <p><%= link_to 'new totp', new_www_app_setting_totp_path %></p>

</div>
