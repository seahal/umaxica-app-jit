<h1><%= t('controller.help.app.contacts.new.page_title') %></h1>
<p>Find me in app/views/www/app/contacts/new.html.erb</p>

<%= form_with model: @service_site_contact, url: root_app_inquiry_email_path(10), method: 'post' do |form| %>
  <% if @service_site_contact.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-md mt-3">
      <h2><%= pluralize(@service_site_contact.errors.count, "error") %> prohibited this sample from being saved:</h2>

      <ul class="list-disc ml-6">
        <% @service_site_contact.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :email_pass_code %>
    <%= form.text_field :email_pass_code %>
  </div>

  <%= form.submit %>
<% end %>
