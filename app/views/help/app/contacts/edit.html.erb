<h1><%= title "Contact request helper" %></h1>

<section class="space-y-6 text-slate-900">
  <p class="text-sm text-slate-600">
    We do not support editing an existing request directly from the portal yet.
    Share the reference below with the support team so they can update the conversation on your behalf.
  </p>

  <%= render "help/shared/contact_overview", contact: @contact %>

  <% if @contact_categories.present? %>
    <div class="rounded-md border border-slate-200 bg-white p-4 shadow-sm">
      <h2 class="text-base font-semibold">Available categories</h2>
      <p class="mt-1 text-sm text-slate-600">Choose the closest match when creating a follow-up request.</p>
      <ul class="mt-3 list-disc space-y-1 pl-4 text-sm text-slate-700">
        <% @contact_categories.each do |category| %>
          <li><%= category.id %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="flex flex-wrap gap-3">
    <%= link_to "Back to details", help_app_contact_path(@contact),
                class: "inline-flex items-center rounded-md border border-slate-300 px-4 py-2 text-sm font-medium text-slate-700 hover:bg-slate-50" %>
    <%= link_to "Create follow-up request", new_help_app_contact_path,
                class: "inline-flex items-center rounded-md bg-slate-900 px-4 py-2 text-sm font-medium text-white hover:bg-slate-800" %>
  </div>
</section>
