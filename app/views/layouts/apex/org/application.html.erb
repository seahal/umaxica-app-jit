<!DOCTYPE html>
<html lang="<%= session[:language]&.downcase || 'ja' %>">
<head>
  <title><%= ENV.fetch('NAME') %> <%= content_for?(:page_title) ? "| #{yield(:page_title)} " : "" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= tenant_stylesheet "apex" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  <link rel="icon" href="https://perm.umaxica.org/jit/favicon.ico" sizes="32x32">
</head>
<body class="px-4">
<header>
  <h1 class="container mx-auto mt-28 px-5 flex">
    <a href="http://<%= ENV['EDGE_STAFF_URL'] %>:4444/"><%= ENV.fetch('NAME') %> (apex, org)</a></h1>
</header>

<hr>

<main class="container mx-auto mt-28 px-5 block">
  <%= yield %>

  <nav>
    <ul>
      <li></li>
    </ul>
    <%= breadcrumbs pretext: "You are here: ", separator: " &rsaquo; " %>
  </nav>
</main>

<nav id="app" class="mt-4 bg-white bg-opacity-50 hover:font-black hover:text-blue-500">
  <nav>
    <% breadcrumbs.tap do |links| %>
      <% if links.any? %>
        <ul>
          <% links.each do |link| %>
            <li> <%= link_to link.text, link.url, class: (link.current? ? "current" : nil) %></li>
          <% end %>
          <li>...</li>
        </ul>
      <% end %>
    <% end %>
  </nav>
</nav>

<hr>

<footer>

  <ul>
    <li><a href="http://<%= ENV['EDGE_STAFF_URL'] %>">home</a></li>
    <li>
      <%= link_to 'Cookie', edit_apex_org_preference_cookie_path %></li>
    <li>Security</li>
    <li>Support</li>
    <li>Terms</li>
    <li>Privacy</li>
  </ul>
  <small>© <%= Time.now.year %> <%= ENV.fetch('NAME') %></small>
</footer>

</body>
</html>
