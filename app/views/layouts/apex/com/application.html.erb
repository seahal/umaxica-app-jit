<!DOCTYPE html>
<html lang="<%= session[:language]&.downcase || 'ja' %>">
<head>
  <title><%= ENV.fetch('NAME') %> <%= content_for?(:page_title) ? "| #{yield(:page_title)} " : "" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  <link rel="icon" href="https://perm.umaxica.com/jit/favicon.ico" sizes="32x32">
</head>

<body class="px-4">
<header>
  <h1><a href="http://<%= ENV['EDGE_CORPORATE_URL'] %>:4444/"><%= ENV.fetch('NAME') %> (apex, com)</a></h1></header>

<hr>

<main class="container mx-auto mt-28 px-5 block">
  <%= yield %>
</main>

<nav>
  <% breadcrumbs.tap do |links| %>
    <% if links.any? %>
      <ul>
        <% links.each do |link| %>
          <li> <%= link_to link.text, link.url, class: (link.current? ? "current" : nil) %></li>
        <% end %>
        <li>...</li>
      </ul>
    <% end %>
  <% end %>
</nav>

<hr>

<footer>
  <ul>
    <li><a href="http://<%= ENV['EDGE_CORPORATE_URL'] %>/">Home</a></li>
    <li><%= link_to 'Support', help_com_root_path %></li>
    <li><%= link_to 'NewsRoom', news_com_root_path %></li>
    <li><%= link_to 'Documents', docs_com_root_path %></li>
    <li><%= link_to 'Cookie', edit_apex_com_preference_cookie_path %></li>
  </ul>
  <small>© <%= Time.now.year %> <%= ENV.fetch('NAME') %></small>
</footer>

</body>
</html>
