<!DOCTYPE html>
<html lang="<%= session[:language]&.downcase || 'ja' %>">
<head>
  <title><%= ENV.fetch('NAME') %> <%= content_for?(:page_title) ? "| #{yield(:page_title)} " : "" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "apex/app/main" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  <link rel="icon" href="https://perm.umaxica.app/jit/favicon.ico" sizes="32x32">
</head>

<body class="px-4">
<header>
  <h1><a href="http://<%= ENV['EDGE_SERVICE_URL'] %>/"><%= ENV.fetch('NAME') %> (apex, app)</a></h1>
</header>

<hr>

<main class="container mx-auto mt-28 px-5 block">
  <%= yield %>
</main>

<nav>
  <% breadcrumbs.tap do |links| %>
    <% if links.any? %>
      <ul>
        <% links.each do |link| %>
          <li> <%= link_to link.text, link.url, class: (link.current? ? "current" : nil) %></li>
        <% end %>
    <% end %>
    <li>...</li>
    </ul>
  <% end %>
</nav>

<hr>

<footer>
  <ul>
    <li><a href="http://<%= ENV['EDGE_SERVICE_URL'] %>/">Home</a></li>
    <li><%= link_to 'Cookie', edit_apex_app_preference_cookie_path %></li>
    <li><%= link_to 'Support', help_app_root_url %></li>
    <li><%= link_to 'NewsRoom', news_app_root_url %></li>
    <li><%= link_to 'Documents', docs_app_root_url %></li>
    <li><%= link_to 'Cookie', edit_apex_app_preference_cookie_path %></li>
  </ul>
  <small>© 2025 <%= ENV.fetch('NAME') %></small>
</footer>

</body>
</html>
