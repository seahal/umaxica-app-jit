<!DOCTYPE html>
<html lang="ja">
<head>
  <title>UMAXICA <%= content_for?(:page_title) ? "| #{yield(:page_title)} " : "" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= tenant_stylesheet "apex" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
</head>
<body class="px-4">
<header>
  <h1 class="container mx-auto mt-28 px-5 flex"><%= ENV.fetch('NAME') %>(www, net)</h1>
</header>

<hr>

<main class="container mx-auto mt-28 px-5 block">
  <%= yield %>
</main>

<nav id="app" class="mt-4 bg-white bg-opacity-50 hover:font-black hover:text-blue-500">
  <nav>
    <% breadcrumbs.tap do |links| %>
      <% if links.any? %>
        <ul>
          <% links.each do |link| %>
            <li> <%= link_to link.text, link.url, class: (link.current? ? "current" : nil) %></li>
          <% end %>
          <li>...</li>
        </ul>
      <% end %>
    <% end %>
  </nav>
</nav>

<hr>

<footer>
  <p>© <%= Time.now.year %> <%= ENV.fetch('NAME') %></p>
</footer>

</body>
</html>
