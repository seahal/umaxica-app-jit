<!DOCTYPE html>
<html lang="<%= get_language %>" data-theme="<%= theme_cookie_value %>" class="<%= theme_html_class %>">
<head>
  <%= display_meta_tags site: "#{ENV['BRAND_NAME']} (com) Documents", title: (content_for(:page_title) if content_for?(:page_title)) %>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="turbo-refresh-method" content="morph">
  <meta name="turbo-refresh-scroll" content="preserve">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "docs/com/main" %>
  <%= javascript_importmap_tags %>
</head>

<body>
<% code_name = ENV.fetch("BRAND_NAME") { ENV.fetch("NAME") } %>
<% edge_host = ENV["EDGE_CORPORATE_URL"].presence %>

<div>    <%= render "shared/flash_messages" %>
  <div>

    <header>
      <div>
        <span>Docs</span>
        <h1>
          <% if edge_host.present? %>
            <a href="http://<%= edge_host %>:4444/"><%= code_name %></a>
          <% else %>
            <%= code_name %>
          <% end %>
          <span>(com)</span>
        </h1>
      </div>
    </header>
<%= render_current_banner(:com) %>
<% if show_cookie_banner? %>
  <%= render "shared/cookie_banner" %>
<% end %>

    <main>
      <%= yield %>
    </main>

    <footer>
      <nav>
        <% if edge_host.present? %>
          <a href="http://<%= edge_host %>:4444/">Umaxica</a>
        <% end %>
        <%= link_to t("docs.com.preferences.footer.preference"), apex_com_preference_url %>
      </nav>
      <%= render "layouts/shared/footer_theme_controls", scope: :com %>
      <div>
        <%= render "shared/copyright" %>
      </div>
    </footer>
  </div>
</div>
</body>
</html>
