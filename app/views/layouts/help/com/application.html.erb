<!DOCTYPE html>
<html lang="ja">
<head>
  <title><%= ENV.fetch('NAME') %> <%= content_for?(:page_title) ? "| #{yield(:page_title)} " : "" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= tenant_stylesheet "help" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  <link rel="icon" href="https://asset.umaxica.net/com/help/favicon.ico" sizes="32x32">
</head>

<body class="px-4">
<header>
  <h1><a href="http://<%= ENV['EDGE_CORPORATE_URL'] %>:4444/"><%= ENV.fetch('NAME') %> (help, com)</a></h1></header>
<hr>

<main class="container mx-auto mt-28 px-5 block">
  <%= yield %>
</main>

<nav>
  <% breadcrumbs.tap do |links| %>
    <% if links.any? %>
      <ul>
        <% links.each do |link| %>
          <li> <%= link_to link.text, link.url, class: (link.current? ? "current" : nil) %></li>
        <% end %>
        <li>...</li>
      </ul>
    <% end %>
  <% end %>
</nav>

<hr>

<footer>
  <ul>
    <li><a href="http://<%= ENV['EDGE_CORPORATE_URL'] %>/">Umaxica</a></li>
    <li><%= link_to 'NewsRoom', news_com_root_url %></li>
    <li><%= link_to 'Document', docs_com_root_url %></li>
    <li><%= link_to 'Help Center', help_com_root_url %></li>
  </ul>
  <p>© <%= Time.now.year %> <%= ENV.fetch('NAME') %></p>
</footer>

</body>
</html>
