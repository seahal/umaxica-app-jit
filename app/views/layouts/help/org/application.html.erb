<!DOCTYPE html>
<html lang="ja">
<head>
  <title><%= ENV.fetch('NAME') %> <%= content_for?(:page_title) ? "| #{yield(:page_title)} " : "" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= tenant_stylesheet "help" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
</head>
<body class="px-4">
<header>
  <h1 class="container mx-auto mt-28 px-5 flex">
    <a href="http://<%= ENV['NEWS_STAFF_URL'] %>/"><%= ENV.fetch('NAME') %> (help, org)</a></h1>
</header>

<hr>

<main class="container mx-auto mt-28 px-5 block">
  <%= yield %>

  <nav>
    <ul>
      <li></li>
    </ul>
    UMAXICA
    <%= breadcrumbs pretext: "You are here: ", separator: " &rsaquo; " %>
  </nav>
</main>

<nav id="app" class="mt-4 bg-white bg-opacity-50 hover:font-black hover:text-blue-500">
  <nav>
    <% breadcrumbs.tap do |links| %>
      <% if links.any? %>
        <ul>
          <% links.each do |link| %>
            <li> <%= link_to link.text, link.url, class: (link.current? ? "current" : nil) %></li>
          <% end %>
          <li>...</li>
        </ul>
      <% end %>
    <% end %>
  </nav>
</nav>

<hr>

<footer>
  <ul>
    <li><a href="http://<%= ENV['EDGE_STAFF_URL'] %>/">Home</a></li>
    <li><%= link_to 'Cookie', edit_apex_org_preference_cookie_path %></li>
    <li><%= link_to 'NewsRoom', news_org_root_url %></li>
    <li><%= link_to 'Document', docs_org_root_url %></li>
    <li><%= link_to 'Help Center', help_org_root_url %></li>
  </ul>
  <p>© <%= Time.now.year %> <%= ENV.fetch('NAME') %></p>
</footer>

</body>
</html>
