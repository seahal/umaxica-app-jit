<!DOCTYPE html>
<html lang="<%= get_language %>" data-theme="<%= theme_cookie_value %>" class="<%= theme_html_class %>">
<head>
  <%= display_meta_tags site: "#{ENV.fetch('BRAND_NAME', 'Umaxica')} (app) Newsroom", title: page_title %>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="turbo-refresh-method" content="morph">
  <meta name="turbo-refresh-scroll" content="preserve">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "news/main", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "news/app/main", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <%= yield :head %>
</head>

<body>

<header>
  <nav>
    <%= render "layouts/shared/flash_messages" %>
    <%= render "layouts/shared/current_banner", tld: :app, domain: :news, region: :jp %>
  </nav>
  <div>
    <h1>
      <% if edge_host.present? %>
        <a href="http://<%= edge_host %>/"><%= ENV.fetch("BRAND_NAME", "Umaxica") %></a>
      <% else %>
        <%= ENV.fetch("BRAND_NAME", "Umaxica") %>
      <% end %>
      <span>(news, app)</span>
    </h1>
  </div>
</header>

<main>
  <%= yield %>
</main>

<footer>
  <nav>
    <ul>
      <% if edge_host.present? %>
        <li><a href="http://<%= edge_host %>/"><%= t("news.app.preferences.footer.home") %></a></li>
      <% end %>
      <li><%= link_to t("news.app.preferences.footer.preference"), sign_app_preference_url %></li>
    </ul>
  </nav>
  <%= render "layouts/shared/footer_cookie_controls", scope: :app %>
  <%= render "layouts/shared/footer_theme_controls", scope: :app %>
  <div>
    <%= render "layouts/shared/copyright" %>
  </div>
</footer>
</body>
</html>
