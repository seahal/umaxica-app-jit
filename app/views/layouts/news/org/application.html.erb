<!DOCTYPE html>
<html lang="ja">
<head>
  <title><%= ENV.fetch('NAME') %> <%= content_for?(:page_title) ? "| #{yield(:page_title)} " : "" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  <link rel="icon" href="https://perm.umaxica.org/jit/favicon.ico" sizes="32x32">
</head>
<body class="px-4">
  <%= render "concern/flash_messages" %>
<header>
  <h1 class="container mx-auto mt-28 px-5 flex">
    <a href="http://<%= ENV['NEWS_STAFF_URL'] %>:4444/"><%= ENV.fetch('NAME') %> (news, org)</a></h1>
</header>

<hr>

<main class="container mx-auto mt-28 px-5 block">
  <%= yield %>
</main>

<hr>

<footer>

  <ul>
    <li><a href="http://<%= ENV['EDGE_STAFF_URL'] %>:4444/">home</a></li>
    <li>
      <%= link_to 'Cookie', edit_apex_org_preference_cookie_path %></li>
    <li>Security</li>
    <li>Support</li>
    <li>Terms</li>
    <li>Privacy</li>
    <li>Accessibility</li>
  </ul>
  <%= render "concern/copyright" %>
</footer>

</body>
</html>
