<!DOCTYPE html>
<html lang="<%= get_language %>">
<head>
  <%= display_meta_tags site: "#{ENV['BRAND_NAME']} (org) Newsroom" %>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="turbo-refresh-method" content="morph">
  <meta name="turbo-refresh-scroll" content="preserve">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "news/main" %>
  <%= stylesheet_link_tag "news/org/main" %>
  <%= javascript_importmap_tags %>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <link rel="icon" href="https://perm.umaxica.org/favicon.ico" sizes="32x32">
</head>
<body class="px-4">
<%= render "concern/flash_messages" %>
<header>
  <h1 class="container mx-auto mt-28 px-5 flex">
    <a href="http://<%= ENV['NEWS_STAFF_URL'] %>:4444/"><%= ENV.fetch('BRAND_NAME') { ENV.fetch('NAME') } %> (news,
      org)</a></h1>
</header>

<hr>

<main class="container mx-auto mt-28 px-5 block">
  <%= yield %>
</main>

<hr>

<footer>

  <ul>
    <li><a href="http://<%= ENV['EDGE_STAFF_URL'] %>:4444/">home</a></li>
    <li>Security</li>
    <li>Support</li>
    <li>Terms</li>
    <li>Privacy</li>
    <li>Accessibility</li>
  </ul>
  <%= render "concern/copyright" %>
</footer>

</body>
</html>
