<!DOCTYPE html>
<html lang="ja">
<head>
  <%= display_meta_tags site: "#{ENV['BRAND_NAME']} (com) Apex" %>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="turbo-refresh-method" content="morph">
  <meta name="turbo-refresh-scroll" content="preserve">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "peak/main" %>
  <%= stylesheet_link_tag "peak/com/main" %>
  <%= javascript_importmap_tags %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
</head>

<body class="px-4">
<% code_name = ENV.fetch("BRAND_NAME") { ENV.fetch("NAME") } %>
<% edge_host = ENV["EDGE_CORPORATE_URL"].presence %>

<div class="mx-auto max-w-6xl px-6">
<header class="flex flex-wrap items-center justify-between gap-4 border-b border-[hsl(var(--border))] py-6">
  <div class="flex flex-col">
    <span class="text-xs font-semibold uppercase tracking-[0.2em] text-[hsl(var(--muted-foreground))]">APEX</span>
    <h1 class="text-2xl font-semibold tracking-tight">
      <% if edge_host.present? %>
        <a class="transition hover:text-[hsl(var(--primary))]" href="http://<%= edge_host %>:4444/">Umaxica(com)</a>
      <% else %>
        Umaxica(com)
      <% end %>
    </h1>
  </div>
</header>

<hr>

<main class="container mx-auto mt-28 px-5 block">
  <%= yield %>
</main>

<nav>

</nav>

<hr>

<footer class="border-t border-[hsl(var(--border))] py-6 text-sm text-[hsl(var(--muted-foreground))]">
  <nav class="flex flex-wrap items-center gap-4">
    <a class="font-semibold text-[hsl(var(--foreground))]" href="https://<%= ENV['EDGE_CORPORATE_URL'] %>"><%= t('peak.com.preferences.footer.home') %></a>
    <%= link_to t('peak.com.preferences.footer.preference'), peak_com_preference_path, class: "font-semibold text-[hsl(var(--foreground))]" %>
    <%= link_to t('peak.com.preferences.footer.privacy'), peak_com_privacy_path, class: "font-semibold text-[hsl(var(--foreground))]" %>
  </nav>
  <div class="mt-4">
    <%= render "concern/copyright" %>
  </div>
</footer>
</div>

</body>
</html>
