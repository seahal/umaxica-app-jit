<!DOCTYPE html>
<html lang="ja">
<head>
  <%= display_meta_tags site: "#{ENV['BRAND_NAME']} (org) JP" %>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="turbo-refresh-method" content="morph">
  <meta name="turbo-refresh-scroll" content="preserve">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "top/org/main" %>
  <%= javascript_importmap_tags %>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
</head>
<body class="px-4">
<header>
  <h1 class="container mx-auto mt-28 px-5 flex">
    <a href="http://<%= ENV['EDGE_STAFF_URL'] %>:4444/">Umaxica(www, org)</a></h1>
  <% unless logged_in? %>
    <ul>
      <li><%= link_to t("auth.app.layout.nav.sign_up"), new_auth_app_registration_path %></li>
      <li><%= link_to t("auth.app.layout.nav.log_in"), new_auth_app_authentication_path %></li>
    </ul>
  <% else %>
    <ul>
      <li><%= link_to t("auth.app.layout.nav.setting"), auth_app_setting_path %></li>
      <li><%= link_to t("auth.app.layout.nav.logout"), auth_app_authentication_path, data: { turbo_method: :delete } %></li>
    </ul>
  <% end %>
</header>

<hr>

<main class="container mx-auto mt-28 px-5 block">
  <%= yield %>
</main>

<nav id="app" class="mt-4 bg-white bg-opacity-50 hover:font-black hover:text-blue-500">
</nav>

<hr>

<footer>
  <ul>
    <li><a href="http://<%= ENV['EDGE_STAFF_URL'] %>:4444/">home</a></li>
    <% if defined?(edit_www_org_preference_cookie_path) %>
      <li><%= link_to 'Cookie', edit_www_org_preference_cookie_path %></li>
    <% end %>
    <li>Security</li>
    <li>Support</li>
    <li>Terms</li>
    <li>Privacy</li>
    <li>© <%= Time.now.year %> Umaxica</li>
  </ul>
  <%= render "concern/copyright" %>
</footer>

</body>
</html>
