<!DOCTYPE html>
<html lang="<%= get_language %>">
<head>
  <%= display_meta_tags site: "#{ENV['BRAND_NAME']} (org)" %>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="turbo-refresh-method" content="morph">
  <meta name="turbo-refresh-scroll" content="preserve">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= javascript_importmap_tags %>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "auth/main" %>
  <%= stylesheet_link_tag "auth/org/main" %>
  <link rel="icon" href="https://perm.umaxica.org/favicon.ico" sizes="32x32">
</head>
<body class="px-4">
<%= render "concern/flash_messages" %>
<header>
  <h1>
    <a href="http://<%= ENV['EDGE_STAFF_URL'] %>/"><%= ENV.fetch('BRAND_NAME') { ENV.fetch('NAME') } %> (sign, org)</a>
  </h1>
  <% unless logged_in? %>
    <ul>
      <li><%= link_to t("auth.org.layout.nav.log_in"), new_auth_org_authentication_path %></li>
    </ul>
  <% else %>
    <ul>
      <li><%= link_to t("auth.org.layout.nav.setting"), auth_org_setting_path %></li>
      <li><%= link_to t("auth.org.layout.nav.logout"), edit_auth_app_exit_url %></li>
    </ul>
  <% end %>
</header>

<hr>

<main class="container mx-auto mt-28 px-5 block">
  <%= yield %>
</main>

<hr>

<footer>

  <ul>
    <li><a href="http://<%= ENV['EDGE_STAFF_URL'] %>:4444/">home</a></li>
  </ul>
  <%= render "concern/copyright" %>
</footer>

</body>
</html>
