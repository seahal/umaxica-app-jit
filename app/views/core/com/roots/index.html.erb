<% code_name = ENV.fetch("BRAND_NAME") { ENV.fetch("NAME") } %>
<% docs_host = ENV["DOCS_CORPORATE_URL"].presence %>
<% help_host = ENV["HELP_CORPORATE_URL"].presence %>

<div class="relative min-h-screen overflow-hidden bg-[hsl(var(--background))] text-[hsl(var(--foreground))]">
  <div class="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_top,_hsl(var(--accent)),_transparent_60%)] opacity-70"></div>

  <div class="relative mx-auto max-w-6xl px-6 py-16">
    <header class="flex flex-col gap-6">
      <span class="inline-flex w-fit items-center gap-2 rounded-full border border-[hsl(var(--border))] bg-[hsl(var(--card))] px-4 py-1 text-xs font-semibold uppercase tracking-[0.2em] text-[hsl(var(--muted-foreground))]">
        Core
      </span>
      <h1 class="text-4xl font-semibold tracking-tight sm:text-5xl">
        <%= code_name %> Corporate
      </h1>
      <p class="max-w-2xl text-base text-[hsl(var(--muted-foreground))] sm:text-lg">
        法人向けのポータルです。連携資料、契約情報、ヘルプへの導線をまとめています。
      </p>
      <div class="flex flex-wrap gap-3">
        <% if docs_host %>
          <%= link_to "Docs", "https://#{docs_host}",
                      class: "rounded-full border border-[hsl(var(--border))] bg-[hsl(var(--card))] px-5 py-2 text-sm font-semibold transition hover:-translate-y-0.5 hover:shadow-lg" %>
        <% end %>
        <% if help_host %>
          <%= link_to "Help", "https://#{help_host}",
                      class: "rounded-full bg-[hsl(var(--primary))] px-5 py-2 text-sm font-semibold text-[hsl(var(--primary-foreground))] transition hover:-translate-y-0.5 hover:shadow-lg" %>
        <% end %>
      </div>
    </header>

    <section class="mt-12 grid gap-6 lg:grid-cols-2">
      <div class="rounded-[var(--radius)] border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6 shadow-[0_20px_60px_-45px_hsl(var(--foreground)/0.25)]">
        <h2 class="text-lg font-semibold">ステータス</h2>
        <dl class="mt-4 grid gap-3 text-sm text-[hsl(var(--muted-foreground))]">
          <div class="flex items-center justify-between">
            <dt>Current language</dt>
            <dd class="font-medium text-[hsl(var(--foreground))]"><%= I18n.locale %></dd>
          </div>
          <div class="flex items-center justify-between">
            <dt>TimeZone</dt>
            <dd class="font-medium text-[hsl(var(--foreground))]"><%= params[:tz].to_s.downcase == "utc" ? "utc" : "jst" %></dd>
          </div>
          <div class="flex items-center justify-between">
            <dt>Hello</dt>
            <dd class="font-medium text-[hsl(var(--foreground))]"><%= I18n.t "hello" %></dd>
          </div>
          <div class="flex items-center justify-between">
            <dt>Tokyo time</dt>
            <dd class="font-medium text-[hsl(var(--foreground))]"><%= Time.current.in_time_zone("Tokyo") %></dd>
          </div>
        </dl>
      </div>

      <div class="rounded-[var(--radius)] border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6">
        <h2 class="text-lg font-semibold">ハイライト</h2>
        <p class="mt-3 text-sm text-[hsl(var(--muted-foreground))]">
          契約関連の案内、運用資料、最新のガイドラインを集約します。
        </p>
        <div class="mt-5 flex flex-wrap gap-2">
          <span class="rounded-full bg-[hsl(var(--secondary))] px-3 py-1 text-xs font-semibold text-[hsl(var(--secondary-foreground))]">Contracts</span>
          <span class="rounded-full bg-[hsl(var(--secondary))] px-3 py-1 text-xs font-semibold text-[hsl(var(--secondary-foreground))]">Policy</span>
          <span class="rounded-full bg-[hsl(var(--secondary))] px-3 py-1 text-xs font-semibold text-[hsl(var(--secondary-foreground))]">Integrations</span>
          <span class="rounded-full bg-[hsl(var(--secondary))] px-3 py-1 text-xs font-semibold text-[hsl(var(--secondary-foreground))]">Support</span>
        </div>
      </div>
    </section>
  </div>
</div>
