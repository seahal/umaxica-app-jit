<% content_for :title, "Showing sample" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Showing sample</h1>

  <%= form_with model: @user_telephone, url: auth_app_registration_telephone_path, method: :patch do |form| %>

    <% if @user_telephone.errors.any? %>
      <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-md mt-3">
        <h2><%= pluralize(@user_telephone.errors.count, "error") %> prohibited this sample from being saved:</h2>

        <ul class="list-disc ml-6">
          <% @user_telephone.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div>
      <%= form.label :pass_code %>
      <%= form.text_field :pass_code %>
    </div>

    <%= form.submit %>
  <% end %>

  <div class="mt-6">
    <%= link_to t("controller.sign.app.registration.telephone.edit.return_page"), new_auth_app_registration_path, class: "inline-flex items-center justify-center px-4 py-2 bg-blue-600 text-white font-semibold rounded-md hover:bg-blue-700 transition" %>
  </div>

</div>
