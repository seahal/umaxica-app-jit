<div class="bg-green-50 text-green-800 px-6 py-4 rounded-lg">
  <h1 class="text-2xl font-bold mb-4">Apple登録成功</h1>
  <p class="mb-4">Appleアカウントでの登録が完了しました。</p>
  
  <% if @user_info %>
    <div class="mb-4">
      <h2 class="text-lg font-semibold mb-2">登録情報:</h2>
      <ul class="list-disc list-inside">
        <li>メール: <%= @user_info[:email] %></li>
        <li>名前: <%= @user_info[:name] %></li>
        <% if @user_info[:first_name] %>
          <li>名: <%= @user_info[:first_name] %></li>
        <% end %>
        <% if @user_info[:last_name] %>
          <li>姓: <%= @user_info[:last_name] %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="mt-6">
    <%= link_to "認証ページに戻る", new_auth_app_authentication_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
  </div>
</div>