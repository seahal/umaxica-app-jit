<h1><%= t('auth.app.withdrawal.new.title', default: 'Withdrawal') %></h1>
<p><%= t('auth.app.withdrawal.new.description', default: 'Are you sure you want to withdraw?') %></p>

<%= form_with url: auth_app_withdrawal_path, method: :post, local: true do |f| %>
  <div>
    <%= f.check_box :confirm_create_recovery_code %>
    <%= f.label :confirm_create_recovery_code, "Confirm create recovery code" %>
  </div>
  <%= render "concern/cloudflare_turnstile" %>
  <%= f.submit t('auth.app.withdrawal.new.submit', default: 'Withdraw'), class: "btn btn-danger" %>
<% end %>
