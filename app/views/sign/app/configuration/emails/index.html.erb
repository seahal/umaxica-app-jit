          <%= link_to t('sign.app.configuration.show.back'), sign_app_configuration_path %>

<div>
  <div>
    <%= link_to t('sign.app.configuration.email.index.new_link'), new_sign_app_configuration_email_path %>
  </div>
</div>

  <div>
    <div>
      <div>
        <div>
          <table>
            <thead>
            <tr>
              <th scope="col">
                <%= t('activerecord.attributes.user_email.address') %>
              </th>
              <th scope="col">
                <%= t('activerecord.attributes.user_email.status') %>
              </th>
              <th scope="col">
                <span>Actions</span>
              </th>
            </tr>
            </thead>
            <tbody>
            <% @user_emails.each do |email| %>
              <tr>
                <td>
                  <%= email.address %>
                </td>
                <td>
                  <% if [UserEmailStatus::VERIFIED, UserEmailStatus::VERIFIED_WITH_SIGN_UP].include?(email.user_email_status_id) %>
                      <span>
                        認証済み
                      </span>
                  <% else %>
                      <span>
                        未認証
                      </span>
                  <% end %>
                </td>
                <td>
                  <%= button_to t("sign.app.configuration.email.index.delete"),
                                sign_app_configuration_email_path(email),
                                method: :delete,
                                data: { turbo_confirm: t("sign.app.configuration.email.index.delete_confirm") } %>
                </td>
              </tr>
            <% end %>
            <% if @user_emails.empty? %>
              <tr>
                <td colspan="3">
                  <div>
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                    </svg>
                    <p>メールアドレスは登録されていません。</p>
                  </div>
                </td>
              </tr>
            <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
