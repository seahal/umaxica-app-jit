<div class="mx-auto max-w-xl">
  <h1 class="text-3xl font-semibold text-slate-900"><%= title t("sign.app.authentication.passkey.new.page_title") %></h1>
  <p class="mt-2 text-sm text-slate-600"><%= t("sign.app.authentication.passkey.new.description") %></p>

  <%= form_with url: sign_app_in_passkey_path, data: { turbo: false }, class: "mt-6 space-y-5 rounded-xl border border-slate-200 bg-white p-6 shadow-sm" do |form| %>

    <div class="flex items-center gap-3">
      <%= form.submit t("sign.app.authentication.passkey.new.submit", default: "パスキーでログイン"),
                      class: "inline-flex items-center rounded-lg bg-slate-900 px-4 py-2 text-sm font-semibold text-white transition hover:bg-slate-800" %>
      <%= render "concern/cloudflare_turnstile" %>
      <%= link_to t("sign.app.authentication.new.back"), new_sign_app_in_path,
                  class: "inline-flex items-center rounded-lg border border-slate-200 px-4 py-2 text-sm font-semibold text-slate-700 transition hover:bg-slate-50" %>
    </div>
  <% end %>
</div>
