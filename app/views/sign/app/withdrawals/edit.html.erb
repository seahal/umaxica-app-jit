<h1><%= t('sign.app.withdrawal.edit.title', default: 'Recover Account') %></h1>
<p><%= t('sign.app.withdrawal.edit.description', default: 'Your account is in withdrawal status. Click below to recover it.') %></p>

<%= form_with url: sign_app_withdrawal_path, method: :patch, local: true do |f| %>
  <div>
    <%= f.check_box :confirm_create_recovery_code %>
    <%= f.label :confirm_create_recovery_code, t('sign.app.withdrawal.edit.recovery_code_label', default: 'Confirm create recovery code') %>
  </div>
  <%= render "concern/cloudflare_turnstile" %>
  <%= f.submit t('sign.app.withdrawal.edit.submit', default: 'Recover Account'), class: "btn btn-primary" %>
<% end %>
