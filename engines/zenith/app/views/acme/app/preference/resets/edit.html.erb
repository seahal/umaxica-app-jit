<% content_for :page_title, t('acme.app.preference.resets.title') %>

<section>
  <h1>
    <%= t("acme.app.preference.resets.title") %>
  </h1>

  <p>
    <%= t("acme.app.preference.resets.description") %>
  </p>

  <%= form_with url: zenith.acme_app_preference_reset_path, method: :delete do |f| %>
    <% if @preference&.errors&.any? %>
      <div>
        <ul>
          <% @preference.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div>
      <%= check_box_tag :confirm_reset, "1", @preference&.confirm_reset == "1", required: true %>
      <%= label_tag :confirm_reset, t("acme.app.preference.resets.confirm") %>
    </div>

    <div>
      <%= f.submit t("acme.app.preference.resets.button"),
                   data: {
                     turbo_submits_with: t("acme.app.preferences.submitting") } %>
      <%= link_to t("acme.app.preference.resets.back"),
                  zenith.acme_app_preference_url %>
    </div>
  <% end %>
</section>
