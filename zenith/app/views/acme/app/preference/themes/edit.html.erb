<% content_for :page_title, t("acme.app.preferences.theme_settings") %>

<section>
  <h1>
    <%= t("acme.app.preference.theme.edit.title") %>
  </h1>
  <p>
    <%= t("acme.app.preference.theme.edit.description") %>
  </p>

  <%= form_with model: @preference_colortheme, scope: :preference_colortheme, url: zenith.acme_app_preference_theme_path, method: :patch do |f| %>
    <div>
      <%= f.label :option_id, t("acme.app.preferences.theme.select_theme") %>
      <div>
        <%= f.select :option_id,
                     options_for_select(
                       [
                         [t("acme.app.preference.theme.edit.options.light"), "light"],
                         [t("acme.app.preference.theme.edit.options.dark"), "dark"],
                         [t("acme.app.preference.theme.edit.options.system"), "system"]
                       ],
                       @preference_colortheme.option&.name || "system"),
                     {} %>
      </div>
    </div>

    <div>
      <%= f.submit t("acme.app.preferences.update_settings"),
                   data: { turbo_submits_with: t("acme.app.preferences.submitting") } %>
      <%= link_to t("acme.app.preferences.back_to_settings"), zenith.acme_app_preference_url %>
    </div>
  <% end %>
</section>
