#
# This file is intentionally lightweight; it simply serves as the bind-mount
# target for devcontainers so that /home/jit/.bashrc is always a file.
# Add personal shell customizations here if needed.
