default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000
  username: <%= ENV["POSTGRES_USER"] %>
  password: <%= ENV["POSTGRES_PASSWORD"] %>


development:
  identifier:
    <<: *default
    database: development_identifier_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5433 %>
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_IDENTIFIER_PUB"]: 'localhost' %>
  identifier_replica:
    <<: *default
    replica: true
    database: development_identifier_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5434 %> # FIXME: change replica
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_IDENTIFIER_SUB"]: 'localhost' %> # FIXME: change to use replica
  account:
    <<: *default
    database: development_account_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5433 %>
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_ACCOUNT_PUB"]: 'localhost' %>
  account_replica:
    <<: *default
    replica: true
    database: development_account_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5434 %> # FIXME: change replica
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_ACCOUNT_SUB"]: 'localhost' %> # FIXME: change to use replica
  session:
    <<: *default
    database: development_session_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5433 %>
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_SESSION_PUB"]: 'localhost' %>
  sesssion_replica:
    <<: *default
    replica: true
    database: development_session_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5434 %> # FIXME: change replica
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_SESSION_SUB"]: 'localhost' %> # FIXME: change to use replica
  storage:
    <<: *default
    database: development_storage_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5433 %>
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_STORAGE_PUB"]: 'localhost' %>
  storage_replica:
    <<: *default
    replica: true
    database: development_storage_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5434 %> # FIXME: change to use replica
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_STORAGE_SUB"]: 'localhost' %>  # FIXME: change to use replica
  message:
    <<: *default
    database: development_message_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5433 %>
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_MESSAGE_PUB"]: 'localhost' %>
  message_replica:
    <<: *default
    replica: true
    database: development_message_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5434 %> # FIXME: change to use replica
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_MESSAGE_SUB"]: 'localhost' %>  # FIXME: change to use replica
  core:
    <<: *default
    database: development_core_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5433 %>
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_CORE_PUB"]: 'localhost' %>
  core_replica:
    <<: *default
    replica: true
    database: development_core_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5434 %> # FIXME: change to use replica
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_CORE_SUB"]: 'localhost' %>  # FIXME: change to use replica


# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  identifier:
    <<: *default
    database: test_identifier_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5433 %>
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_IDENTIFIER_PUB"]: 'localhost' %>
  identifier_replica:
    <<: *default
    replica: true
    database: test_identifier_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5434 %>
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_IDENTIFIER_SUB"]: 'localhost' %> # FIXME: change to use replica
  account:
    <<: *default
    database: test_account_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5433 %>
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_ACCOUNT_PUB"]: 'localhost' %>
  account_replica:
    <<: *default
    replica: true
    database: test_account_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5434 %>
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_ACCOUNT_SUB"]: 'localhost' %> # FIXME: change to use replica
  session:
    <<: *default
    database: test_session_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5433 %>
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_SESSION_PUB"]: 'localhost' %>
  session_replica:
    <<: *default
    replica: true
    database: test_session_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5434 %>
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_SESSION_SUB"]: 'localhost' %>  # FIXME: change to use replica
  storage:
    <<: *default
    database: test_storage_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5433 %>
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_STORAGE_PUB"]: 'localhost' %>
  storage_replica:
    <<: *default
    replica: true
    database: test_storage_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5434 %>
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_STORAGE_SUB"]: 'localhost' %>  # FIXME: change to use replica
  message:
    <<: *default
    database: test_message_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5433 %>
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_MESSAGE_PUB"]: 'localhost' %>
  message_replica:
    <<: *default
    replica: true
    database: test_message_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5434 %>
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_MESSAGE_SUB"]: 'localhost' %> # FIXME: change to use replica
  core:
    <<: *default
    database: test_core_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5433 %>
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_CORE_PUB"]: 'localhost' %>
  core_replica:
    <<: *default
    replica: true
    database: test_core_db
    port:
      <%= File.exist?('/.dockerenv') ? 5432: 5434 %>
    host:
      <%= File.exist?('/.dockerenv') ? ENV["POSTGRESQL_CORE_SUB"]: 'localhost' %>  # FIXME: change to use replica

# FIXME: please implement below
production:
  none:
    <<: *default
    database: production_db
    port: 5432
    host: localhost
