development:
  adapter: solid_cable
  silence_polling: true
  polling_interval: 0.1.seconds
  connects_to:
    database:
      writing: cable
      reading: cable_replica
test:
  adapter: test
production:
  adapter: solid_cable
  silence_polling: true
  polling_interval: 0.1.seconds
  connects_to:
    database:
      writing: cable
      reading: cable_replica
